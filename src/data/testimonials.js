// Static replacement for the Base44 `Testimonial` entity. The Testimonials
// section is currently disabled in src/pages/Home.jsx — no reviews were ever
// published through Base44, so this starts empty. To enable it: add entries
// here (shape: { id, client_name, quote, rating, service, display_order })
// and uncomment the <Testimonials /> import/usage in Home.jsx.
export const testimonials = [];
